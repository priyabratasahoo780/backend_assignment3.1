# backend_assignment3.1
A fully functional RESTful API built with Node.js and Express.js implementing complete CRUD operations (GET, POST, PUT, PATCH, DELETE) with dynamic routing, proper HTTP status codes, and clean REST architecture.
